enum SignInType {
  facebook,
  google,
}

enum TextFieldType {
  name,
  email,
  password,
  phoneNumber,
  multiline,
}

enum ViewType {
  grid,
  list,
}

enum FeedbackType {
  bugReport,
  productFeedback,
  contentReleaseFeedback,
  contentCooperationFeedback,
  anotherProblem,
}